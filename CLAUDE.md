# Claude Code 開発設定

## 🚫 サーバー運用ルール

### ⛔ 絶対禁止事項
以下の操作は**絶対に実行しない**でください：

1. **サーバーの停止・再起動**
   - 開発サーバー（`npm run dev`）の停止
   - 本番サーバーの停止
   - プロセスの中断

2. **ビルドプロセスの中断**
   - `npm run build` 実行中のキャンセル
   - ビルドエラーが発生してもプロセスを強制終了しない

3. **環境の破壊的変更**
   - `.next` フォルダの不適切な削除
   - `node_modules` の不適切な削除
   - 設定ファイルの無断変更

4. **不要なビルド・キャッシュ操作の実行**
   - 開発者の許可なくビルドを強制で実行しない
   - ハードキャッシュの削除（ブラウザまたは `.next` の強制クリア）を勝手に行わない

5. **サーバー停止の禁止（追記）**
   - サーバー（開発/本番）の停止は原則禁止。どうしても必要な場合は事前に担当者の承認を得ること

### ✅ 許可される操作
- コードの編集・修正
- 新機能の実装
- バグの修正
- 最適化の実施
- テストの実行

## プロジェクト情報

- **作業対象**: C:\multi chat\multi-chat
- **混同禁止**: C:\ai-chat-v3\ai-chat-app-new とは別プロジェクト
- **現在のブランチ**: feature/ui-improvements

## 現在進行中のタスク

リファクタリング計画 Phase 3: DOM操作の除去
- broadcast-input.tsx の DOM直接操作を CSS/Tailwind に置換
- forceButtonStyles() 関数の削除
- setTimeout 呼び出しの削除